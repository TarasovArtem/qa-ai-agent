name: Cypress E2E Tests

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

# Safe minimal baseline: read the repo. Each job below adds only the extra
# permission it actually needs (actions: read for cypress-tests' flaky-
# history lookup; pull-requests: write for qa-ai-triage's PR comment) -
# see that job's own `permissions:` block rather than a workflow-wide
# grant every job inherits whether it needs it or not.
permissions:
  contents: read

jobs:
  # Pure-JS tests for scripts/ai/ (provider contract, MockProvider,
  # GroqProvider against a fake fetch, retry/error normalization, prompt
  # building, etc.) - see package.json's test:unit script for the exact
  # file glob. Deliberately its own job, not a step inside cypress-tests:
  # it needs no browser, no live app, and no `needs:` on the Cypress
  # matrix - it tests scripts/ai/ in isolation, so it starts immediately
  # and runs in parallel with Cypress - chrome/edge. Job id/name are both
  # fixed strings (no matrix), since branch protection's required status
  # checks match on the exact check name.
  unit-tests:
    name: Unit tests
    runs-on: ubuntu-latest
    # Scoped down from the workflow-level permissions block above (which
    # exists for the Cypress jobs' GitHub Actions history/PR-comment
    # calls) to the one thing this job actually does: read the repo to
    # check it out. No GitHub API access, no PR comments, no secrets.
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      # Runs entirely offline: MockProvider makes no network requests, and
      # GroqProvider's own tests inject a fake fetch instead of calling the
      # real Groq API - no GROQ_API_KEY/AI_API_KEY is passed to this job,
      # so there is nothing here to authenticate with even if a test tried
      # to. No continue-on-error - a failing unit test must fail this job.
      - name: Run unit tests
        run: npm run test:unit

  # Standalone, offline, informational check for the QA Agent evaluation
  # infrastructure (Dataset v1 + Baseline v1 + regression.js): reports
  # classification/action accuracy and a REGRESSED/IMPROVED/UNCHANGED
  # comparison against the frozen baseline in the job log. Deliberately its
  # own job with no `needs:` on Cypress or qa-ai-triage - it only reads
  # repository files (dataset.json, baseline-v1.json) and Node dependencies,
  # so it starts immediately and runs in parallel with them, exactly like
  # unit-tests above. For v1-v5 (QA-failure-triage evaluation), a REGRESSED
  # result is informational only (see regression*.js's run()/
  # formatRegressionReport*()) and must not fail this job.
  #
  # v6 (Roadmap #22G, Test Design quality evaluation - a different subject
  # than v1-v5, see scoring-v6.js's own header comment) is a deliberate
  # exception, hardened in #22G-C1: v6 is a STRICT REVIEWED-BASELINE DRIFT
  # GATE, not merely "regressions fail while improvements pass freely."
  # regression-v6.js's run() returns exitCode 1 for REGRESSED, IMPROVED,
  # AND BASELINE_MISMATCH alike - only an exact match to the committed,
  # human-reviewed baseline-v6.json (status UNCHANGED) exits 0. An
  # "improvement" still requires a human to review and explicitly update
  # the committed baseline in a normal, diffed commit - it is never an
  # automatic pass, because an always-pass scorer mutation can look
  # identical to a genuine content improvement from the comparator's own
  # point of view (see regression-v6.js's own header comment for the full
  # rationale). A negative/sentinel case scoring poorly is NOT itself a
  # regression (its committed baseline-v6.json entry already records that
  # expected weak status, so it compares as UNCHANGED against itself); only
  # a DIVERGENCE from that committed expectation, in either direction,
  # exits non-zero.
  #
  # For every version, a technical failure (invalid dataset/baseline, a
  # runtime crash, an unexpected non-zero exit) always fails this job via
  # each npm script's own exit code - no continue-on-error is used.
  # Fully offline: no GROQ_API_KEY/AI_API_KEY, no AI_PROVIDER/AI_MODEL, no
  # AI provider or network call of any kind.
  qa-agent-evaluation:
    name: QA Agent evaluation
    runs-on: ubuntu-latest
    # Scoped down to the one thing this job does: read the repo to check it
    # out. No GitHub API access, no PR comments, no secrets.
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run QA Agent evaluation
        run: npm run eval:ai

      - name: Run QA Agent regression comparison
        run: npm run eval:regression

      - name: Run QA Agent evaluation v2
        run: npm run eval:ai:v2

      - name: Run QA Agent regression comparison v2
        run: npm run eval:regression:v2

      - name: Run QA Agent evaluation v3
        run: npm run eval:ai:v3

      - name: Run QA Agent regression comparison v3
        run: npm run eval:regression:v3

      - name: Run QA Agent evaluation v4
        run: npm run eval:ai:v4

      - name: Run QA Agent regression comparison v4
        run: npm run eval:regression:v4

      - name: Run QA Agent evaluation v5
        run: npm run eval:ai:v5

      - name: Run QA Agent regression comparison v5
        run: npm run eval:regression:v5

      - name: Run Test Design quality evaluation v6
        run: npm run eval:ai:v6

      - name: Run Test Design quality regression comparison v6
        run: npm run eval:regression:v6

  cypress-tests:
    name: Cypress - ${{ matrix.browser }}
    runs-on: ubuntu-latest
    # contents: read for checkout; actions: read so "Collect run history
    # for flaky-test reasoning" (below) can query the Actions API for past
    # workflow runs. This job no longer posts or updates PR comments (see
    # qa-ai-triage) so it does not need pull-requests: write.
    permissions:
      contents: read
      actions: read
    # cypress/included bundles Node, npm and matching pre-installed browser
    # binaries for the same Cypress version declared in package.json, so no
    # separate Node setup or binary caching step is needed.
    container:
      image: cypress/included:15.19.0
      options: --shm-size=1gb
    strategy:
      fail-fast: false
      # Firefox is excluded here: running it inside this nested container
      # (a Docker container on top of the Actions runner's own container)
      # reliably hangs during WebDriver session creation and times out
      # after ~9-10 min, regardless of shm-size, seccomp, or retries —
      # a known class of Firefox-in-Docker sandboxing limitation, not a
      # config issue in this repo. Run it locally instead: npm run firefox.
      matrix:
        browser: [chrome, edge]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Install dependencies
        run: npm ci

      # baseUrl in cypress.config.js points at the externally hosted app,
      # so there is nothing to build or serve locally before running tests.
      #
      # continue-on-error lets the job carry on into the diagnostics steps
      # below instead of stopping here. steps.run_tests.outcome records the
      # real pass/fail result (unaffected by continue-on-error, which only
      # overrides .conclusion) - the "Fail job on E2E failure" step at the
      # end of this job reads it to make sure the job still ends up failed.
      - name: Run Cypress E2E tests (${{ matrix.browser }})
        id: run_tests
        continue-on-error: true
        run: npm run ${{ matrix.browser }}

      # Everything below this point is additive diagnostics. None of it can
      # turn a failed E2E run into a passing job: continue-on-error above
      # only lets these steps execute, and steps.run_tests.outcome (not the
      # ambient failure()/success() functions, which would already read as
      # "success" once a continue-on-error step fails) is what the final
      # gate step below checks.

      # Machine-readable pass/fail marker for this specific browser leg,
      # written unconditionally (always(), not just on failure) so the
      # downstream QA AI triage job can tell "chrome passed" apart from
      # "chrome's result is simply unknown" - see
      # scripts/ai/aggregate-browser-context.js. Deliberately never
      # computed by AI: it's a direct, literal read of Cypress's own
      # recorded step outcome.
      - name: Record browser result
        if: always()
        run: |
          mkdir -p reports/ai
          printf '{"browser": "%s", "framework": "cypress", "outcome": "%s"}' "${{ matrix.browser }}" "${{ steps.run_tests.outcome }}" > reports/ai/browser-result.json

      - name: Collect structured failure context
        if: steps.run_tests.outcome == 'failure'
        env:
          TEST_BROWSER: ${{ matrix.browser }}
        run: npm run ai:collect

      # Best-effort flaky-test signal: aggregated pass/fail counts for this
      # browser's job over its last 10 completed runs on main (GitHub
      # Actions run/job history via the API - no artifact downloads, no
      # external database). GITHUB_TOKEN is the default per-run token, not
      # a custom secret, so this runs the same for fork and same-repo PRs.
      # Entirely optional: any failure here (first-ever run, rate limit,
      # API error) writes { available: false } and this step still
      # succeeds - the downstream QA AI triage job works with or without it.
      - name: Collect run history for flaky-test reasoning
        if: steps.run_tests.outcome == 'failure'
        env:
          GITHUB_TOKEN: ${{ github.token }}
          TEST_BROWSER: ${{ matrix.browser }}
        run: npm run ai:history

      - name: Upload Cypress screenshots
        if: steps.run_tests.outcome == 'failure'
        uses: actions/upload-artifact@v7
        with:
          name: cypress-screenshots-${{ matrix.browser }}
          path: cypress/screenshots
          if-no-files-found: ignore

      - name: Upload Cypress videos
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: cypress-videos-${{ matrix.browser }}
          path: cypress/videos
          if-no-files-found: ignore

      - name: Upload structured test report
        if: steps.run_tests.outcome == 'failure'
        uses: actions/upload-artifact@v7
        with:
          name: cypress-report-${{ matrix.browser }}
          path: reports/cypress
          if-no-files-found: ignore

      # Browser-scoped input for the downstream QA AI triage job - NOT the
      # final AI analysis itself (that no longer runs in this job at all,
      # see qa-ai-triage below). Uploaded unconditionally so browser-
      # result.json (always written above) reaches triage even for a
      # passing browser; context.json/history.json are only ever present
      # here when this leg actually failed, and if-no-files-found: ignore
      # means their absence on a passing run is not an error. Artifact
      # name is unique per matrix leg (qa-triage-input-chrome vs.
      # qa-triage-input-edge) so the two legs' uploads can never collide.
      - name: Upload QA triage input
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: qa-triage-input-${{ matrix.browser }}
          path: reports/ai
          if-no-files-found: ignore

      # The one step in this job allowed to actually fail it. Everything
      # above ran (or was skipped) without affecting job status; this is
      # the sole source of truth for whether the job conclusion is
      # failure, and it depends only on the original test outcome - never
      # on whether context/history collection succeeded, failed, or was
      # skipped, and never on the downstream AI triage job (which hasn't
      # even started yet when this runs).
      - name: Fail job on E2E failure
        if: steps.run_tests.outcome == 'failure'
        run: |
          echo "Cypress E2E tests failed for browser '${{ matrix.browser }}'. See the report/screenshot/video artifacts (and QA AI triage, if it runs) for details."
          exit 1

  # Firefox (Roadmap #14C), deliberately its own job rather than a third
  # matrix.browser entry above: Firefox reliably hung during WebDriver
  # session creation inside the cypress-tests job's nested
  # cypress/included Docker container (see that job's matrix comment) -
  # not an application/test defect, a container-sandboxing limitation
  # specific to that execution environment. Roadmap #14B's CI spike
  # (PR #49) proved the fix is environmental, not architectural: running
  # directly on the bare ubuntu-latest runner, with Firefox installed
  # explicitly rather than bundled in a container, executes the exact
  # same unmodified test suite successfully (10/10 passing, ~80s total,
  # no hang). Chrome/Edge above are intentionally left on their proven
  # container-based setup - there is no evidence requiring all three
  # browsers to share one execution environment, and changing a setup
  # that already works would be pure, unproven risk.
  #
  # Every step below mirrors cypress-tests' own diagnostics/artifacts/
  # failure-semantics exactly (same artifact names via the "-firefox"
  # suffix, same continue-on-error/steps.run_tests.outcome contract, same
  # "Fail job on E2E failure" gate) so the downstream aggregator and QA AI
  # triage job need zero awareness that this leg's execution environment
  # differs from chrome/edge's.
  firefox-tests:
    name: Cypress - firefox
    runs-on: ubuntu-latest
    # Same permissions as cypress-tests, for the same reasons: contents:
    # read for checkout, actions: read for "Collect run history for
    # flaky-test reasoning" below.
    permissions:
      contents: read
      actions: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      # cypress-tests doesn't need this step - cypress/included bundles
      # Node/npm already. This job runs on the bare runner, so it needs
      # its own Node setup, identical to unit-tests/qa-agent-evaluation/
      # qa-ai-triage above.
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      # The one step this job has that no other job needs: cypress/included
      # bundles Firefox for chrome/edge's container; this job installs it
      # explicitly on the bare runner instead, exactly as proven in the
      # Roadmap #14B spike. "latest" mirrors the spike's own proven choice
      # rather than introducing a new, untested version-pinning policy.
      - name: Set up Firefox
        uses: browser-actions/setup-firefox@v1
        with:
          firefox-version: latest

      # TEMPORARY - Roadmap #19.7F-B4B (Firefox stability investigation).
      # Runs on every Firefox job, pass or fail, before the original test
      # execution below - read-only safe metadata capture only, never
      # touches reports/cypress, cypress/screenshots, or cypress/videos, so
      # it cannot affect anything the original run or its existing
      # diagnostics steps read/write. Fixes the historical gap (#19.7F-B4A)
      # where runner ImageOS/ImageVersion and exact Firefox/Cypress versions
      # were never captured on production runs. Removal: delete this step
      # once Firefox root cause is captured/confirmed or the investigation
      # is explicitly closed.
      - name: "Capture Firefox runtime metadata (temporary, #19.7F-B4B)"
        if: always()
        run: |
          echo "=== #19.7F-B4B temporary runtime metadata (safe subset only) ==="
          date -u +"%Y-%m-%dT%H:%M:%SZ"
          echo "ImageOS=${ImageOS:-<unset>}"
          echo "ImageVersion=${ImageVersion:-<unset>}"
          echo "RUNNER_OS=${RUNNER_OS:-<unset>}"
          echo "RUNNER_ARCH=${RUNNER_ARCH:-<unset>}"
          uname -a
          node --version
          npm --version
          npx cypress version
          firefox --version
          which firefox
          free -m
          df -h /dev/shm || true
          uptime

      # Identical contract to cypress-tests' own "Run Cypress E2E tests"
      # step: continue-on-error lets the job carry on into the diagnostics
      # steps below instead of stopping here; steps.run_tests.outcome (not
      # affected by continue-on-error, which only overrides .conclusion)
      # is what "Fail job on E2E failure" below actually checks. No spec
      # filtering, no Firefox-specific flags - this is the exact same
      # "firefox" script chrome/edge's own npm run ${{ matrix.browser }}
      # would use if it were still in that matrix.
      #
      # UNCHANGED by #19.7F-B4B: same id, same continue-on-error, same
      # command, same semantics as before this roadmap stage. This step's
      # own outcome is the sole authority for the job's pass/fail result -
      # nothing added below this file in #19.7F-B4B can alter that.
      - name: Run Cypress E2E tests (firefox)
        id: run_tests
        continue-on-error: true
        run: npm run firefox

      # Everything below this point is additive diagnostics, identical in
      # spirit to cypress-tests' own copy - none of it can turn a failed
      # E2E run into a passing job.

      - name: Record browser result
        if: always()
        run: |
          mkdir -p reports/ai
          printf '{"browser": "firefox", "framework": "cypress", "outcome": "%s"}' "${{ steps.run_tests.outcome }}" > reports/ai/browser-result.json

      - name: Collect structured failure context
        if: steps.run_tests.outcome == 'failure'
        env:
          TEST_BROWSER: firefox
        run: npm run ai:collect

      - name: Collect run history for flaky-test reasoning
        if: steps.run_tests.outcome == 'failure'
        env:
          GITHUB_TOKEN: ${{ github.token }}
          TEST_BROWSER: firefox
        run: npm run ai:history

      - name: Upload Cypress screenshots
        if: steps.run_tests.outcome == 'failure'
        uses: actions/upload-artifact@v7
        with:
          name: cypress-screenshots-firefox
          path: cypress/screenshots
          if-no-files-found: ignore

      - name: Upload Cypress videos
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: cypress-videos-firefox
          path: cypress/videos
          if-no-files-found: ignore

      - name: Upload structured test report
        if: steps.run_tests.outcome == 'failure'
        uses: actions/upload-artifact@v7
        with:
          name: cypress-report-firefox
          path: reports/cypress
          if-no-files-found: ignore

      # Same "browser-scoped input for the downstream QA AI triage job"
      # contract as cypress-tests' own copy - qa-triage-input-firefox can
      # never collide with -chrome/-edge, and its presence/absence is
      # exactly what aggregate-browser-context.js's readBrowserInputs()
      # already knows how to handle for any browser in its list.
      - name: Upload QA triage input
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: qa-triage-input-firefox
          path: reports/ai
          if-no-files-found: ignore

      # Identical gate to cypress-tests' own copy: the sole source of
      # truth for this job's conclusion, depending only on the original
      # test outcome - never on diagnostics collection or the downstream
      # AI triage job.
      - name: Fail job on E2E failure
        if: steps.run_tests.outcome == 'failure'
        run: |
          echo "Cypress E2E tests failed for browser 'firefox'. See the report/screenshot/video artifacts (and QA AI triage, if it runs) for details."
          exit 1

      # TEMPORARY - Roadmap #19.7F-B4B (Firefox stability investigation).
      # Placed deliberately LAST, after every existing step in this job
      # (including "Fail job on E2E failure" above) so that by the time
      # this runs, the ORIGINAL run's reports/cypress, cypress/screenshots,
      # and cypress/videos have already been fully consumed by the
      # unchanged "Collect structured failure context" (ai:collect),
      # "Upload Cypress screenshots", "Upload structured test report", and
      # "Upload Cypress videos" steps above - so nothing this step does can
      # collide with or corrupt the existing AI-triage input or artifact
      # pipeline. Only runs when the ORIGINAL npm run firefox execution
      # already failed; that result is already final by this point and
      # cannot be changed by anything below. continue-on-error: true is
      # redundant with the script's own unconditional `exit 0` but stated
      # explicitly so a reviewer never has to wonder whether a forensic
      # script bug could mask or alter the real result.
      #
      # Removal: delete this step, the next step, and
      # scripts/diagnostics/firefox-failure-forensics.sh once Firefox root
      # cause is captured/confirmed or the investigation is explicitly
      # closed (see README/roadmap #19.7F-B4A/#19.7F-B4B).
      #
      # #19.7F-B4B-FIX: `if: steps.run_tests.outcome == 'failure'` alone is
      # NOT sufficient here. GitHub Actions implicitly prepends `success()`
      # to any custom `if:` expression that doesn't already call an explicit
      # status-check function - and "Fail job on E2E failure" immediately
      # above has already failed (no continue-on-error, real `exit 1`)
      # whenever this step's own condition would be true, which makes the
      # implicit `success()` false and would silently skip this step exactly
      # when it's needed. `always()` overrides that implicit gating so the
      # explicit outcome check below is what actually decides whether this
      # runs - independent review #19.7F-B4B-R found and confirmed this.
      - name: "Preserve and analyze Firefox failure evidence (temporary, #19.7F-B4B)"
        if: always() && steps.run_tests.outcome == 'failure'
        continue-on-error: true
        run: bash scripts/diagnostics/firefox-failure-forensics.sh

      # Separate, distinctly-named artifact - never read by qa-ai-triage
      # (which only downloads qa-triage-input-chrome/edge/firefox) and never
      # overlapping reports/ai/*, so this cannot introduce an extra browser
      # failure signal or an extra AI provider call. Short retention because
      # this is temporary debugging output, not a permanent record.
      #
      # #19.7F-B4B-FIX: same implicit-success() reasoning as the step above -
      # `always()` is required or this upload is silently skipped whenever
      # the job has already failed, which is precisely when it must run.
      - name: "Upload Firefox forensics artifact (temporary, #19.7F-B4B)"
        if: always() && steps.run_tests.outcome == 'failure'
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          name: firefox-forensics
          path: reports/firefox-forensics
          if-no-files-found: ignore
          retention-days: 7

  # Playwright (Roadmap #21G): the first real Playwright execution in
  # GitHub Actions. Deliberately its own job, not folded into cypress-tests'
  # matrix - Playwright is an explicit, opt-in framework (Roadmap #21E),
  # never Cypress's zero-configuration default, and mixing a
  # QA_FRAMEWORK=playwright leg into a matrix whose other legs are
  # implicitly Cypress would blur that boundary. Exactly one Chromium
  # project, one worker, zero retries (see playwright.config.js) - no
  # Firefox/WebKit, no browser matrix. Every step below mirrors
  # cypress-tests'/firefox-tests' own continue-on-error/
  # steps.run_tests.outcome contract, so a failing smoke still uploads its
  # evidence and still fails this job.
  playwright-tests:
    name: Playwright Chromium
    runs-on: ubuntu-latest
    # contents: read for checkout only - this job never calls the GitHub
    # Actions API and never needs a provider secret (AI_API_KEY is only
    # ever passed to qa-ai-triage below, never here).
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      # Chromium only (Roadmap #21G invariant: no Firefox/WebKit, no
      # browser matrix) - --with-deps installs the Linux system libraries
      # Chromium needs on a bare ubuntu-latest runner, mirroring
      # firefox-tests' own explicit-browser-install pattern (there is no
      # existing Playwright container image already in use here, unlike
      # cypress-tests' cypress/included).
      - name: Install Playwright Chromium browser
        run: npx playwright install --with-deps chromium

      # G21G-1 (mandatory): explicit --config=playwright.config.js, never
      # implicit discovery - Playwright's own config discovery is
      # cwd-dependent when --config is absent. One project (chromium), one
      # worker, zero retries are all locked in playwright.config.js itself
      # (see its own module comment) - this command only locks the config
      # file and project explicitly, it does not repeat those values.
      #
      # continue-on-error lets the job carry on into the evidence-upload
      # steps below instead of stopping here - identical in spirit to
      # cypress-tests'/firefox-tests' own "Run Cypress E2E tests" contract.
      # steps.run_tests.outcome (unaffected by continue-on-error, which
      # only overrides .conclusion) is what "Fail job on smoke failure"
      # below actually checks.
      - name: Run Playwright Chromium smoke
        id: run_tests
        continue-on-error: true
        run: npm run test:e2e:playwright

      # Evidence upload happens regardless of pass/fail (if: always()) so a
      # failing smoke's report/screenshots are never lost - mirrors
      # cypress-tests'/firefox-tests' own "Upload Cypress videos" contract.
      # if-no-files-found: ignore means a setup/browser-install failure
      # that never produced a report is not itself an upload error - only
      # real evidence is ever uploaded here, nothing synthesized.
      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: playwright-report
          path: |
            reports/playwright/report.json
            reports/playwright/test-results
          if-no-files-found: ignore

      # The sole source of truth for this job's conclusion - identical
      # contract to cypress-tests'/firefox-tests' own "Fail job on E2E
      # failure" gate, depending only on the original smoke outcome, never
      # on artifact upload success or the downstream triage job.
      - name: Fail job on smoke failure
        if: steps.run_tests.outcome == 'failure'
        run: |
          echo "Playwright Chromium smoke failed. See the playwright-report artifact for details."
          exit 1

  # Runs downstream of the full Cypress matrix (chrome, edge, and - since
  # Roadmap #14C - firefox), at most once per workflow run - the whole
  # point of this job existing (see PR #23) is that a failure appearing in
  # more than one browser must trigger exactly one real Groq request, not
  # one per browser. `needs: [cypress-tests, firefox-tests]` is required
  # for the same reason it was required for chrome/edge alone: without
  # firefox-tests listed here too, this job would not wait for Firefox at
  # all, and a Firefox-only failure would never reach centralized triage.
  # `if: always()` is required here: GitHub Actions skips a downstream
  # `needs:` job by default once any dependency job fails, and AI triage
  # specifically needs to run *especially* when Cypress failed - so this
  # job must always get the chance to start. The actual "is there anything
  # worth analyzing" decision is made inside the job (aggregate-browser-
  # context.js's shouldRunAiTriage()), never by this `if:` condition, and
  # never by an LLM - it's a plain boolean over Cypress's own recorded
  # outcomes.
  #
  # Diagnostic, not authoritative: this job is intentionally NOT part of
  # branch protection's required checks (Cypress - chrome/edge already
  # are; Cypress - firefox is deliberately not required either, at least
  # initially - see README). AI analysis has never been able to turn a
  # failed Cypress run green, and that stays true here - see "Fail job on
  # E2E failure" above, which this job has no influence over.
  qa-ai-triage:
    name: QA AI triage
    needs: [cypress-tests, firefox-tests, playwright-tests]
    if: always()
    runs-on: ubuntu-latest
    # contents: read for checkout; pull-requests: write to post/update the
    # single triage PR comment below. Roadmap #21H: actions: read added -
    # unlike Cypress (collect-history.js runs per-browser inside
    # cypress-tests/firefox-tests, which already have this permission),
    # Playwright's own job never runs ai:history itself (see "Collect
    # Playwright context" above for why: only this centralized triage job
    # does Playwright-specific AI evidence collection), so this job needs
    # the same GitHub Actions history API access those jobs already have -
    # reusing the exact same permission scope, not a broader one.
    permissions:
      contents: read
      pull-requests: write
      actions: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      # Each browser leg's own artifact from cypress-tests' "Upload QA
      # triage input" step above. continue-on-error: true - a browser job
      # that never reached its upload step (e.g. cancelled) shouldn't hard
      # -fail triage; aggregate-browser-context.js treats a missing
      # browser input as simply unknown for that browser, not a crash.
      - name: Download chrome triage input
        uses: actions/download-artifact@v7
        continue-on-error: true
        with:
          name: qa-triage-input-chrome
          path: reports/ai/browser-inputs/chrome

      - name: Download edge triage input
        uses: actions/download-artifact@v7
        continue-on-error: true
        with:
          name: qa-triage-input-edge
          path: reports/ai/browser-inputs/edge

      # Same contract as chrome/edge above - firefox-tests uploads this
      # artifact unconditionally (always()), so its absence here only ever
      # means that job never reached its own upload step.
      - name: Download firefox triage input
        uses: actions/download-artifact@v7
        continue-on-error: true
        with:
          name: qa-triage-input-firefox
          path: reports/ai/browser-inputs/firefox

      # Roadmap #21G: Playwright's own outcome, recorded directly from
      # needs.playwright-tests.result (a native GitHub Actions job
      # conclusion: success/failure/cancelled/skipped) rather than an
      # uploaded browser-result.json - the Playwright job itself never
      # writes to reports/ai/ at all (see playwright-tests above), so
      # there is nothing to download for this part. readBrowserInputs()
      # already treats any outcome other than "success"/"failure" as
      # absent data (see aggregate-browser-context.js), so a cancelled or
      # skipped Playwright job is handled by the exact same pre-existing
      # logic chrome/edge/firefox already rely on - no new filtering
      # needed here.
      #
      # Roadmap #21G-C1: "framework": "playwright" is a static literal
      # this workflow step itself knows to be true - never inferred by
      # aggregate-browser-context.js from the "playwright-chromium" job
      # name string. This is what lets that script keep Playwright's
      # outcome out of Cypress's own browserCorrelation (and vice versa):
      # a Playwright pass/fail is workflow-level evidence about an
      # independent, unrelated smoke test, never same-test evidence that
      # the Cypress suite also passed/failed in "another browser".
      - name: Record Playwright browser result
        if: always()
        run: |
          mkdir -p reports/ai/browser-inputs/playwright-chromium
          printf '{"browser": "playwright-chromium", "framework": "playwright", "outcome": "%s"}' "${{ needs.playwright-tests.result }}" > reports/ai/browser-inputs/playwright-chromium/browser-result.json

      # The RAW reporter artifact from playwright-tests, downloaded to
      # exactly the adapter's own default path (reports/playwright/
      # report.json - see playwright-adapter.js's DEFAULT_REPORT_FILE) so
      # no reportFile override is ever needed. continue-on-error: true - a
      # Playwright job that never reached its own upload step (setup
      # failure, cancellation) simply leaves nothing here, handled safely
      # by the next step's own "no report found" path, never a crash.
      - name: Download Playwright report evidence
        uses: actions/download-artifact@v7
        continue-on-error: true
        with:
          name: playwright-report
          path: reports/playwright

      # Roadmap #21G: unlike Cypress's own per-browser legs, the
      # Playwright job itself never runs ai:collect (see playwright-tests
      # above) - only this centralized triage job does, and only when
      # Playwright's own recorded outcome was a genuine failure, so this
      # never runs twice and never runs speculatively for a passing leg.
      # QA_FRAMEWORK=playwright routes through the exact same
      # runtime-framework-selector.js -> playwright-adapter.js path any
      # other QA_FRAMEWORK=playwright invocation uses (Roadmap #21E) -
      # this step never sets metadata.framework directly. If the
      # downloaded report is genuinely absent (a setup/browser-install
      # failure before any test ran), the Playwright adapter already
      # produces an honest testResults.found:false / failedTests:[]
      # context (never fabricated evidence - see playwright-adapter.js's
      # loadReport()), and analyze-failure.js's own main() already skips
      # the AI provider entirely whenever failedTests is empty - so this
      # can never cause a hallucinated Playwright test failure to reach
      # the model. The cp below only populates this one browser's own
      # input slot (reports/ai/browser-inputs/playwright-chromium/
      # context.json) - it does not decide anything by itself; "Aggregate
      # browser inputs" below still owns the actual pick-one decision and
      # will overwrite reports/ai/context.json with whichever browser it
      # actually selects.
      - name: Collect Playwright context (only if the Playwright job itself failed)
        if: needs.playwright-tests.result == 'failure'
        env:
          QA_FRAMEWORK: playwright
          TEST_BROWSER: chromium
        run: |
          npm run ai:collect
          cp reports/ai/context.json reports/ai/browser-inputs/playwright-chromium/context.json

      # Roadmap #21H: the same bounded, optional flaky-test signal Cypress's
      # own "Collect run history for flaky-test reasoning" step already
      # produces (see cypress-tests above), now also produced for
      # Playwright - same semantic History contract, same collect-history.js
      # producer, no separate schema. HISTORY_JOB_NAME is required because
      # Playwright's real GitHub Actions job name ("Playwright Chromium") is
      # a fixed string, not the Cypress - <browser> template collect-
      # history.js falls back to when this env var is absent (every existing
      # Cypress call site never sets it). QA_FRAMEWORK: playwright routes
      # through the exact same selectRuntimeAdapter() resolution used
      # everywhere else in this pipeline, so the written record's own
      # `framework` field is "playwright", never a duplicated literal.
      # Entirely optional and best-effort, exactly like Cypress's own step:
      # any failure here (first-ever run, rate limit, API error) writes
      # { available: false } and this step still succeeds - the downstream
      # analysis works with or without it. Same job-status condition as
      # "Collect Playwright context" above (only when Playwright's own run
      # genuinely failed) - this never runs speculatively for a passing leg,
      # and never triggers a second Playwright test execution (it only reads
      # GitHub Actions run/job history via the API, not the SUT).
      - name: Collect Playwright history (only if the Playwright job itself failed)
        if: needs.playwright-tests.result == 'failure'
        env:
          GITHUB_TOKEN: ${{ github.token }}
          TEST_BROWSER: playwright-chromium
          HISTORY_JOB_NAME: "Playwright Chromium"
          QA_FRAMEWORK: playwright
        run: |
          npm run ai:history
          cp reports/ai/history.json reports/ai/browser-inputs/playwright-chromium/history.json

      # Reads every browser's browser-result.json, decides whether ANY E2E
      # failure happened, and if so, deterministically writes ONE browser's
      # context.json/history.json to the exact paths analyze-failure.js
      # already reads - see scripts/ai/aggregate-browser-context.js for the
      # full "pick one, don't merge" contract. reports/ai/context.json
      # existing afterward is this step's own signal for whether analysis
      # is needed, exposed as a step output for the steps below.
      - name: Aggregate browser inputs
        id: aggregate
        run: |
          node scripts/ai/aggregate-browser-context.js
          if [ -f reports/ai/context.json ]; then
            echo "has-failures=true" >> "$GITHUB_OUTPUT"
          else
            echo "has-failures=false" >> "$GITHUB_OUTPUT"
          fi

      # Same provider-neutral AI analysis this workflow has always run
      # (see scripts/ai/providers/) - only its location changed: it used
      # to run once per browser (up to 2x per workflow run), now it runs
      # at most once, here, for whichever browser aggregate-browser-
      # context.js selected as primary. GROQ_API_KEY is the GitHub
      # repository secret; AI_API_KEY is the generic, provider-neutral
      # application env var GroqProvider actually reads (see
      # scripts/ai/config.js) - analyze-failure.js itself never learns
      # Groq's name. Deliberately no fallback to MockProvider if
      # GROQ_API_KEY is missing/invalid (e.g. a fork PR, where GitHub does
      # not expose repository secrets to fork-triggered pull_request runs -
      # AI_API_KEY simply resolves empty there): a real-provider request
      # that can't authenticate should fail honestly (surfacing as "AI
      # analysis unavailable" in the artifact/PR comment via
      # continue-on-error below), not silently produce a fabricated
      # analysis. continue-on-error: an AI provider failure is this job's
      # own infrastructure problem, not an E2E result, and must never
      # affect this job's conclusion or anything about Cypress's own
      # required checks.
      - name: AI failure analysis
        if: steps.aggregate.outputs.has-failures == 'true'
        continue-on-error: true
        env:
          AI_PROVIDER: groq
          AI_MODEL: openai/gpt-oss-120b
          AI_API_KEY: ${{ secrets.GROQ_API_KEY }}
        run: npm run ai:analyze

      # Moved here unchanged from the old per-browser "Comment AI analysis
      # on PR" step - format-pr-comment.js and pr-comment-client.js are
      # untouched, only which browser drives the comment changed (read
      # from ai-report.json's own sourceContext.browser, since this job
      # isn't part of a browser matrix and has no matrix.browser of its
      # own). At most one comment per workflow run now, for whichever
      # browser was selected as primary.
      #
      # Fork PRs: GitHub does not expose repository secrets (GROQ_API_KEY)
      # to fork-triggered pull_request runs, so AI_API_KEY resolves empty
      # there - GroqProvider's own config validation rejects that as a
      # CONFIGURATION error (no fallback to MockProvider), and this step's
      # continue-on-error means that shows up as "AI analysis unavailable",
      # not a crash and not a leaked secret. Same-repo PRs get a real
      # Groq-backed report. Either way, reports/ai/ai-report.json may or
      # may not exist by this point - what is separately blocked for forks
      # is posting the *comment*: GitHub forces GITHUB_TOKEN to read-only
      # for fork-triggered pull_request runs regardless of the
      # `permissions:` block above, so the pull-requests: write comment API
      # call would 403 - that's caught and logged as a warning, not a job
      # failure. Intentionally not switching to pull_request_target to work
      # around this: that trigger runs workflow code from the base branch
      # against the fork's PR content, which is a materially different
      # trust boundary and out of scope for this change. Security wins over
      # getting a comment on every fork PR.
      - name: Comment AI analysis on PR
        if: steps.aggregate.outputs.has-failures == 'true' && github.event_name == 'pull_request'
        continue-on-error: true
        uses: actions/github-script@v9
        with:
          script: |
            const fs = require('fs');
            const path = require('path');

            const reportPath = path.join(process.env.GITHUB_WORKSPACE, 'reports', 'ai', 'ai-report.json');
            if (!fs.existsSync(reportPath)) {
              core.notice('No reports/ai/ai-report.json (AI analysis could not be completed, or the run failed before this step) - skipping PR comment.');
              return;
            }

            let report;
            try {
              report = JSON.parse(fs.readFileSync(reportPath, 'utf8'));
            } catch (err) {
              core.warning(`Could not parse ai-report.json: ${err.message}`);
              return;
            }

            if (!Array.isArray(report.results) || report.results.length === 0) {
              core.notice('ai-report.json has no results - skipping PR comment.');
              return;
            }

            const prNumber = context.payload.pull_request && context.payload.pull_request.number;
            if (!prNumber) {
              core.notice('No pull_request number in the event payload - skipping PR comment.');
              return;
            }

            const { formatComment, MARKER } = require(
              path.join(process.env.GITHUB_WORKSPACE, 'scripts', 'ai', 'format-pr-comment.js')
            );
            const { upsertPrComment } = require(
              path.join(process.env.GITHUB_WORKSPACE, 'scripts', 'ai', 'pr-comment-client.js')
            );
            const browser = (report.sourceContext && report.sourceContext.browser) || 'unknown';
            const runUrl = `${process.env.GITHUB_SERVER_URL}/${process.env.GITHUB_REPOSITORY}/actions/runs/${process.env.GITHUB_RUN_ID}`;
            const body = formatComment({ browser, report, runUrl });
            const marker = MARKER(browser);

            try {
              // One comment per browser, identified by its hidden marker,
              // updated in place on retry instead of piling up new ones.
              const result = await upsertPrComment({
                github,
                owner: context.repo.owner,
                repo: context.repo.repo,
                issueNumber: prNumber,
                marker,
                body,
              });
              core.notice(`${result.action === 'created' ? 'Created' : 'Updated'} QA Agent comment for ${browser} (id ${result.id}).`);
            } catch (err) {
              // Most likely a fork PR, where GITHUB_TOKEN is forced
              // read-only - not a reason to fail the job.
              core.warning(`Could not post/update the PR comment: ${err.message}`);
            }

      # Mirror image of the step above: once every browser is passing
      # again, replace any previously-posted triage comment with a short
      # "resolved" note instead of leaving a stale failure analysis on the
      # PR forever. This job doesn't track across runs which single
      # browser was "primary" the last time there was a failure, so it
      # checks every known browser's marker and resolves whichever one(s)
      # actually exist - a browser that never had a comment simply finds
      # nothing and is skipped.
      - name: Resolve QA Agent comment on PR
        if: steps.aggregate.outputs.has-failures == 'false' && github.event_name == 'pull_request'
        continue-on-error: true
        uses: actions/github-script@v9
        with:
          script: |
            const path = require('path');

            const prNumber = context.payload.pull_request && context.payload.pull_request.number;
            if (!prNumber) {
              return;
            }

            const { formatResolvedComment, MARKER } = require(
              path.join(process.env.GITHUB_WORKSPACE, 'scripts', 'ai', 'format-pr-comment.js')
            );
            const { findMarkedComment } = require(
              path.join(process.env.GITHUB_WORKSPACE, 'scripts', 'ai', 'pr-comment-client.js')
            );
            const runUrl = `${process.env.GITHUB_SERVER_URL}/${process.env.GITHUB_REPOSITORY}/actions/runs/${process.env.GITHUB_RUN_ID}`;

            for (const browser of ['chrome', 'edge', 'firefox', 'playwright-chromium']) {
              const marker = MARKER(browser);
              try {
                const existing = await findMarkedComment({
                  github,
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issueNumber: prNumber,
                  marker,
                });
                if (!existing) {
                  continue; // this browser never had a triage comment on this PR
                }

                const body = formatResolvedComment({ browser, runUrl });
                await github.rest.issues.updateComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  comment_id: existing.id,
                  body,
                });
                core.notice(`Marked QA Agent comment for ${browser} as resolved (id ${existing.id}).`);
              } catch (err) {
                core.warning(`Could not resolve the PR comment for ${browser}: ${err.message}`);
              }
            }

      # Aggregated/selected context, history, and the final ai-report.json
      # (if AI analysis produced one) - not the raw per-browser downloads
      # in reports/ai/browser-inputs/ (those are already preserved as each
      # browser's own qa-triage-input-<browser> artifact from cypress-tests).
      # if-no-files-found: ignore means a Groq failure (no ai-report.json)
      # still uploads context.json/history.json rather than losing them.
      - name: Upload AI analysis artifacts
        if: steps.aggregate.outputs.has-failures == 'true'
        uses: actions/upload-artifact@v7
        with:
          name: ai-report
          path: |
            reports/ai/context.json
            reports/ai/history.json
            reports/ai/ai-report.json
          if-no-files-found: ignore
